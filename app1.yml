apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
spec:
  replicas: 6
  selector:
    matchLabels:
      app: httpd
  template:
    metadata:
      labels:
        app: httpd
    spec:
      containers:
      - name: httpd
        image: httpd:alpine
        resources:
          request:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
